import { FileOutput } from 'graphql-codegen-core';
import { CLIOptions } from './cli-options';
import { Logger } from 'ts-log';
interface GenerateOptions extends CLIOptions {
    logger?: Logger;
    templateConfig?: {
        [key: string]: any;
    };
}
export declare function generate(options: GenerateOptions, saveToFile?: boolean): Promise<FileOutput[] | any>;
export {};
